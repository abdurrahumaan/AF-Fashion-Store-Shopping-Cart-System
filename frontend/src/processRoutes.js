import UserLogin from "layouts/Auth"

const processRoutes = [{
        path: "/userLogin",
        name: "User Login",
        icon: "pe-7s-graph",
        component: UserLogin,
        layout: "/user"
    },

];

export default processRoutes